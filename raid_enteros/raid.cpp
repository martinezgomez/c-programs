#include<stdio.h>
#include<stdlib.h>

#define N 8

void show_me(int A[N][N]){

for(int fila = 0; fila<N; fila++


    int A[N][N];
    int inicial = 0;


  int main(int argc, char *argv[]){
    for(int fila=0; fila<N; fila++){
      for(int col=0; col<N; col++)
	printf("%3i", A[fila][col] = ++inicial);
      printf("\n");
    }
  }
  show_me(A);







  return EXIT_SUCCESS;
}
